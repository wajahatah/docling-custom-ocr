[pytest]
markers =
    api: tests that require the FastAPI server to be running on localhost:8100
    plugin: tests that exercise the OCR plugin directly (no server needed)
    slow: full Docling pipeline tests (GPU/CPU inference, slow)
